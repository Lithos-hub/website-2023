import { createTheme } from "@mui/material/styles";

export const mainTheme = createTheme({
  palette: {
    primary: {
      main: "#ffedd8",
    },
    secondary: {
      main: "#ffcb69",
    },
  },
});
